# Dependencias del backend POS (instalar dentro de un entorno virtual).
# Uso: python -m venv .venv && source .venv/bin/activate (o .venv\Scripts\activate en Windows)
#      pip install -r requirements.txt
fastapi>=0.109.0,<0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.25
alembic>=1.13.1
pydantic[email]>=2.6.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.0.0
python-multipart>=0.0.9
psycopg2-binary>=2.9.9
aiosqlite>=0.19.0
pandas>=2.0.0              # Para importación masiva desde Excel
openpyxl>=3.1.0            # Dependencia de pandas para leer .xlsx

# ── PDF Generation ───────────────────────────────────────────────────────────
reportlab>=4.1.0           # Generación de PDF (Ficha de Recepción)
pillow>=10.0.0             # Procesamiento de imágenes para PDF

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-html>=4.1.0
httpx>=0.27.0          # Requerido por TestClient async de FastAPI
